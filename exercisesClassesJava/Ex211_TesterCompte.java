package exercisesRevisionClasses;

import java.util.Scanner;
import java.util.ArrayList;

public class Ex211_TesterCompte {
	public static void main(String[] args) {
		Scanner input = new Scanner(System.in);
	
		
		/*   question 1	
	*	Compte c1 = new Compte("Petros");
	*	Compte c2 = new Compte("Myrsini");
	*	c1.deposer(500);
	*	c2.deposer(1000);
	*	c2.retirer(10);
	*	c1.virerVers(75, c2);
	*	c1.getSolde();
	*	c2.getSolde();
	*/
		
		
	/*
	*	ArrayList<Compte> tab = new ArrayList<Compte>();
	*	System.out.println("un nom...: ");
	*	String nom = input.nextLine();
	*	Compte c1 = new Compte(nom);
	*	tab.add(c1);
	*	System.out.println("un nom...: ");
	*	nom = input.nextLine();
	*	Compte c2 = new Compte(nom);
	*	tab.add(c2);
	*	System.out.println("un nom...: ");
	*	nom = input.nextLine();
	*	Compte c3 = new Compte(nom);
	*	tab.add(c3);
	*	System.out.println("un nom...: ");
	*	nom = input.nextLine();
	*	Compte c4 = new Compte(nom);
	*	tab.add(c4);
	*	System.out.println("un nom...: ");
	*	nom = input.nextLine();
	*	Compte c5 = new Compte(nom);
	*	tab.add(c5);
	*	for (int i = 0; i < tab.size(); i++) {
	*		tab.get(i).deposer(200 + (i * 100));
	*		
	*	}
	*	for (int i = 0; i < tab.size(); i++) {
	*		for (int j = i; j < tab.size();  j++) {
	*			if ( j < tab.size() ) {
	*				tab.get(i).virerVers(20, tab.get(j));
	*			}
	*		}
	*	}
	*	for (int i = 0; i < tab.size(); i++) {
	*		System.out.println("Compte numero : " );
	*		tab.get(i).getSolde();
	*	}
	*/
		
		
	
	}
	
}

